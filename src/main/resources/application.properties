ingestion.schedule=0 * * * * *

# Application Configuration
spring.application.name=payrep

# Logging Configuration
logging.level.org.springframework.security=DEBUG

# JWT Configuration
# IMPORTANT: replace dev secret in production
jwt.secret=dev-insecure-secret-change-me-32bytes-min-please-change-1234abcd
jwt.validity.ms=3600000
